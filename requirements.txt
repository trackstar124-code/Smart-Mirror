# Week 1
requests                          # making HTTP calls to the weather API
opencv-python; sys_platform == "darwin"  # macOS: GUI-capable cv2. On Pi/Linux, start.sh installs apt python3-opencv (GTK) instead — do NOT pip-install OpenCV there or it shadows the GTK build and breaks cv2.imshow.
python-dotenv                     # loads variables from a .env file into the environment

# Week 2
flask             # web framework: serves the dashboard page to the browser

# Week 3
onnxruntime       # runs ONNX hand-landmark & palm-detector models (ARM64-native)
numpy             # tensor pre/post-processing for ONNX inference
